// ------------- HEADER SECTION -------------


/** # SetInterval #
 * @brief Python setInterval wrapper
 * Python wrapper for the native setInterval function
 * $$ app.SetInterval(callback, interval?) $$
 * @param {fnc_json} callback {}
 * @param {num_mls} [interval]
*/
